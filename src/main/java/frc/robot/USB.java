package frc.robot;

/**
 * {@summary}
 * This holds all of the static constant values on the USB that we are using.
 */
public class USB {
    /**
     * {@summary}
     * This holds all of the controller ports we are using.
     */
    public static final class CONTROLLER {
        public static final byte DRIVER = 0;
    }
}
